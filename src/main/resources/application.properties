spring.application.name=IceCream-SpringBoot
server.port=8080

# Para MySQL, cambiamos localhost a mysql (nombre del servicio en docker-compose)
#spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:DB_IceCream}
#spring.datasource.username=${MYSQL_USER:root}
#spring.datasource.password=${MYSQL_PASSWORD:Oso17}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Para MongoDB, cambiamos localhost a mongo (nombre del servicio en docker-compose)
#spring.data.mongodb.uri=mongodb://${MONGO_HOST:mongo}:${MONGO_PORT:27017}/${MONGO_DB:heladeria_db}

# Bases de datos en la nube
# Configuración de MySQL (Azure Flexible Server)
spring.datasource.url=${SPRING_DATA_MYSQL_URI:jdbc:mysql://icecream.mysql.database.azure.com:3306/icecream_db?useSSL=true&requireSSL=false}
spring.datasource.username=${MYSQL_USER:dilan}
spring.datasource.password=${MYSQL_PASSWORD:Admin#0717}
# Configuración de MongoDB (Atlas)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://dilan:mQ9Cs3o8WurKkY96@cluster0.8xrtd6b.mongodb.net/heladeria_db?retryWrites=true&w=majority}
